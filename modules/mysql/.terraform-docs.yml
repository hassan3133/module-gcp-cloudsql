version: ">= 0.13, < 1.0.0"

formatter: markdown table
header-from: ../../examples/mysql/ha/single_instances_with_ha.tf

sections:
  show:
    - header
    - inputs
    - resources
    - modules
    - outputs

output:
  file: README.md
  mode: inject

settings:
  indent: 4
  escape: false
  default: true
  required: true
  type: true

sort:
  enabled: true
  by: required
